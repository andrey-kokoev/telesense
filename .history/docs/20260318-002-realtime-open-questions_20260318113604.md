# Cloudflare Realtime Protocol Open Questions

Status definitions:

- **open**: Not yet verified against working example or official spec
- **verified**: Confirmed via captured payload or authoritative documentation
- **discarded**: Question invalidated by new information (document why)

## Session Management

| Question                                                          | Status | Source/Notes                                                                               |
| ----------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------ |
| Exact `sessions/new` request body (empty vs required fields)      | open   | Docs issue #28488 indicates ambiguity around request body; verify if TTL/metadata optional |
| Exact `sessions/new` response paths (`result.id`, `result.token`) | open   | Inferred from CF API patterns; verify in Echo Demo capture                                 |
| Whether sessions have explicit TTL or auto-expire                 | open   | Mentioned in docs; exact mechanism unverified                                              |

## Track Publish

| Question                                                                            | Status | Source/Notes                                                                                          |
| ----------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------- |
| Exact `tracks/new` request body schema                                              | open   | Fields like `trackName`, `kind`, `location` documented but exact shape unverified                     |
| Whether `tracks/new` batches tracks (array) or single track per call                | open   | Lifecycle diagram shows "tracks" plural; verify batch behavior in capture                             |
| Whether `location: "local"` is the only option or remote tracks also specified      | open   | May affect remote-subscribe request shape                                                             |
| Whether `trackName` must be unique within session                                   | open   | Uniqueness requirement affects app-generated naming strategy                                          |
| Whether `bidirectionalMediaStream` field is required                                | open   | Docs issue #28488 reports it may be required for sendrecv; test with/without in Echo Demo             |
| Exact `tracks/new` response paths for track IDs                                     | open   | `result.tracks[].id` vs `result.trackId` dual-shape guess; verify which path is canonical             |
| Whether `tracks/new` returns SDP answer in same response or requires follow-up call | open   | Lifecycle shows `newTracksResponse (Answer)`; verify exact field path `result.sessionDescription.sdp` |

## Subscribe/Receive Flow (Critical Blocker)

| Question                                                                                          | Status | Source/Notes                                                                                                                                  |
| ------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Exact remote-subscribe negotiation mechanism                                                      | open   | **Critical blocker for 1:1**: How does existing peer receive Offer when remote joins?                                                         |
| How existing browser receives remote offer (Pattern B delivery)                                   | open   | Hypotheses: backend polling `GET /session`, WebSocket/SSE push, or proxy detection via `tracks/new` response; verify in Echo Demo network tab |
| Whether subscription requires explicit track IDs in `renegotiate` body or inferred from SDP alone | open   | Currently guessing SDP-only; verify if `trackSubscription` field exists in renegotiate payload                                                |

## Renegotiate

| Question                                                                               | Status | Source/Notes                                                                             |
| -------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| Exact `renegotiate` request body schema                                                | open   | `sessionDescription` with `type: "answer"` confirmed directionally; other fields unknown |
| Exact `renegotiate` response shape                                                     | open   | Assumed minimal (`{"result":"ok"}` or HTTP 204); verify actual payload                   |
| Whether `renegotiate` can be called multiple times safely (add/remove tracks mid-call) | open   | Affects mute/unmute implementation strategy                                              |

## Cleanup

| Question                                                                | Status | Source/Notes                                                                              |
| ----------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------- |
| Exact `tracks/close` payload (array of track IDs vs single track ID)    | open   | Endpoint documented at `PUT /tracks/close`; body schema not detailed in available sources |
| Whether explicit session close exists or cleanup is track-by-track only | open   | Verify if session-level end operation exists beyond individual track close                |

## ICE/Connectivity

| Question                                                              | Status | Source/Notes                                                                                                                  |
| --------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------- |
| Whether Trickle ICE is supported, unsupported, or merely undocumented | open   | Docs issue reporter guessed "unsupported" due to missing documentation; verify in ICE candidate timing capture from Echo Demo |
| Whether vanilla ICE (gather-all-then-signal) is required              | open   | Verify if candidates must be gathered before `tracks/new` or if trickle is accepted                                           |

## Next Action

**Run Echo Demo**, capture network traffic in DevTools, populate this document with verbatim JSON payloads, move items from `open` to `verified`.

**Highest Priority**: Remote-subscribe negotiation mechanism (how existing browser receives SFU-generated Offer when remote peer joins). Without this, 1:1 receive flow cannot be implemented.
