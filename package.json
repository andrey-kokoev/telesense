{
  "name": "telesense-monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter telesense dev",
    "dev:meter": "pnpm --filter usage-meter dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter telesense test",
    "test:e2e": "pnpm --filter telesense test:e2e",
    "ship": "./scripts/ship.sh",
    "ship:telesense": "pnpm --filter telesense ship",
    "ship:meter": "pnpm --filter usage-meter ship",
    "deploy": "pnpm run ship",
    "deploy:telesense": "pnpm --filter telesense ship",
    "deploy:meter": "pnpm --filter usage-meter ship",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250301.0",
    "prettier": "^3.2.0",
    "typescript": "^5.3.0",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.32.1"
}
